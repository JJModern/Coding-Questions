#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t; cin >> t;

    vector<int> result(t);

    for (int i = 0; i < t; i ++) {
        cin << a;
    }

    for (int num : result) {
        cout << num << endl;
    }
}

// #include <string>
// #include <deque>

// sort(result.begin(), result.end())