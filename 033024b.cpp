#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t; cin >> t;

    vector<int> result(t);

    for (int i = 0; i < t; i ++) {
        cin << a << b << m;
        // can be done mathematically

        // if m < a, b

        // if a, b < m

        // else:
    }

    for (int num : result) {
        cout << num << endl;
    }
}